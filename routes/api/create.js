import { Router } from "express"

const router = Router();
router.post("/", (req, res) => {
    /**
     * TODO: Youtube URL Upload/Processing
     */
    res.send("Youtube URL Upload/Processing");
})
export default router;